using System.Collections;
using UnityEngine;

namespace SnakesNLadders.Assets.Scripts.Pooler
{
    public class SnakeCollisionActivate : MonoBehaviour
    {
        /*private bool _isTriggered;

        public delegate void PoolerActivate();
        public static event PoolerActivate OnPoolerActivate;

        private void OnTriggerEnter2D(Collider2D collision)
        {
            if (!_isTriggered)
            {
                _isTriggered = true;
                OnPoolerActivate?.Invoke();
                Debug.Log("it is collision about activate pooler");
            }
        }

        public void Triggered()
        {
            _isTriggered = false;
        }*/
    }
}